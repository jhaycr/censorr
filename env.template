# Copy this file to .env and adjust values

# Image selection removed; compose builds locally by default

# Timezone
TZ=UTC

# User/Group IDs (if needed for permissions)
UID=1000
GID=1000

# Media host paths
MEDIA_PATH_TV=/mnt/media/tv
MEDIA_PATH_MOVIES=/mnt/media/movies

# Work and config directories
WORKDIR_PATH=/srv/censorr/work
CONFIG_PATH=/srv/censorr/config

# Verbose logging
CENSORR_VERBOSE=false
